# Business OS Environment Variables

# Production Deployment: Repository Root Path
# Required for production deployments (Cloudflare Pages, Vercel, etc.)
# Must be an absolute path to the monorepo root
# Example: BUSINESS_OS_REPO_ROOT=/app
#
# Development: Automatically inferred from process.cwd()
# (strips /apps/business-os suffix if present)
BUSINESS_OS_REPO_ROOT=
