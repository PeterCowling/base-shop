# Business OS Environment Variables

# Production Deployment: Repository Root Path
# Required for production deployments (Cloudflare Pages, Vercel, etc.)
# Must be an absolute path to the monorepo root
# Example: BUSINESS_OS_REPO_ROOT=/app
#
# Development: Automatically inferred from process.cwd()
# (strips /apps/business-os suffix if present)
BUSINESS_OS_REPO_ROOT=

# Authentication (MVP-B1)
# Enable invite-only authentication system
# Default: false (disabled for backward compatibility)
# Set to "true" to require login for all pages
BUSINESS_OS_AUTH_ENABLED=false

# Session Secret
# Required when BUSINESS_OS_AUTH_ENABLED=true
# Must be at least 32 characters long
# Generate with: openssl rand -base64 32
# Example: SESSION_SECRET=your-secret-key-at-least-32-chars-long
SESSION_SECRET=

# Admin Impersonation (optional)
# Allow admins to switch users for testing/support
# Default: false (user switcher hidden when auth enabled)
# Set to "true" to show user switcher to admins
NEXT_PUBLIC_BUSINESS_OS_ALLOW_ADMIN_IMPERSONATION=false

# Repository Write Lock (MVP-C1)
# Enable file-based write lock to serialize git operations
# Default: false (disabled for backward compatibility / single-user mode)
# Set to "true" to prevent git corruption under concurrent write load
# Required when BUSINESS_OS_AUTH_ENABLED=true for multi-user deployments
BUSINESS_OS_REPO_LOCK_ENABLED=false
