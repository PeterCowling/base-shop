# Utilities

- All utility functions must be pure functions with explicit TypeScript types.
- Every utility should have a corresponding test file in `src/utils/__tests__`.
- For linting and testing commands, see the repository root `AGENTS.md`.
