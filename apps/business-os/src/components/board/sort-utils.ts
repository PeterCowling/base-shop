/**
 * @deprecated Use `orderCards` from `@/lib/board-logic` instead (BOS-14)
 *
 * This file is kept for backwards compatibility but is no longer used.
 * All sorting logic has been consolidated into board-logic.ts.
 */

import { orderCards } from "@/lib/board-logic";

export { orderCards as sortCards };
