// apps/cms/tsconfig.test.json         ← create or edit

{
  "extends": "./tsconfig.json", // already there
  "compilerOptions": {
    // 🟢  ONLY add *extra* keys here – do not copy the whole "paths" map
    "types": ["@testing-library/jest-dom", "jest", "node"],
    "noEmit": true
  },
  "include": ["src/**/*", "__tests__/**/*"]
}
