/**
 * JSON-LD structured data components for CochlearFit.
 *
 * Uses @acme/seo/jsonld builders for Schema.org-compliant structured data
 * with XSS-safe serialization.
 */
export {
  faqJsonLd,
  JsonLdScript,
  organizationJsonLd,
  productJsonLd,
} from "@acme/seo/jsonld";
