/* eslint-disable ds/no-raw-color, ds/no-raw-font */
import type { ThemeAssets } from "@themes/base";

/**
 * Cochlearfit brand assets — minimal stub.
 *
 * All cochlearfit tokens are HSL triplets managed via derivedVars.light
 * in theme-css-config.ts. No hex brand colors, fonts, gradients, shadows,
 * or keyframes are defined here; those live in globals.css directly.
 */
export const assets: ThemeAssets = {
  fonts: {},
  gradients: {},
  shadows: {},
  keyframes: {},
  brandColors: {},
};
