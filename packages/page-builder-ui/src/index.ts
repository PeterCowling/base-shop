// Public surface for Page Builder React UI.
// Export React components/hooks from here so consumers import only from
// `@acme/page-builder-ui`, not from deep src/** paths.
export const version = "0.0.0-dev";
