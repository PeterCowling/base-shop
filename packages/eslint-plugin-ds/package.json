{
  "name": "@acme/eslint-plugin-ds",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b",
    "test": "JEST_FORCE_CJS=1 jest --ci --runInBand --detectOpenHandles --config ../../jest.config.cjs",
    "clean": "rimraf dist *.tsbuildinfo",
    "lint": "eslint ."
  },
  "peerDependencies": {
    "eslint": "^9"
  }
}
