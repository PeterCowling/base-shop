{
  "schema_version": "do-stage-handoff.v2",
  "stage": "plan",
  "feature_slug": "inventory-uploader-user-attribution",
  "source_artifact_path": "docs/plans/inventory-uploader-user-attribution/plan.md",
  "source_artifact_status": "Active",
  "source_last_updated": "2026-03-13T00:00:00.000Z",
  "execution_track": "code",
  "deliverable_type": "code-change",
  "primary_execution_skill": "lp-do-build",
  "supporting_skills": [
    "none"
  ],
  "related_artifacts": {
    "analysis": "docs/plans/inventory-uploader-user-attribution/analysis.md"
  },
  "outcome_contract": {
    "why": "When stock goes missing or an error is spotted, there is no record of who made the change. Attaching the logged-in user to every stock adjustment makes it easy to audit who did what and catch mistakes.",
    "intended_outcome_type": "operational",
    "intended_outcome_statement": "Every stock change record shows which staff member made it, enabling accountability and easier error investigation.",
    "source": "operator"
  },
  "engineering_coverage": [
    {
      "coverage_area": "UI / visual",
      "applicable": "N/A — server-side only",
      "current_or_option_state": "-",
      "gap_or_implication": "No UI changes",
      "carry_forward_or_notes": null
    },
    {
      "coverage_area": "UX / states",
      "applicable": "N/A — API response is additive",
      "current_or_option_state": "-",
      "gap_or_implication": "Adding `operatorId` field is backward-compatible",
      "carry_forward_or_notes": null
    },
    {
      "coverage_area": "Security / privacy",
      "applicable": "IP derived server-side, never from request body; actor identity cannot be fabricated by caller",
      "current_or_option_state": "TASK-01",
      "gap_or_implication": "`null` stored gracefully when proxy headers disabled",
      "carry_forward_or_notes": null
    },
    {
      "coverage_area": "Logging / observability / audit",
      "applicable": "`operatorId` written to every `InventoryAuditEvent` row; visible in ledger GET response",
      "current_or_option_state": "TASK-01",
      "gap_or_implication": "Core deliverable of the change",
      "carry_forward_or_notes": null
    },
    {
      "coverage_area": "Testing / validation",
      "applicable": "TC-01 through TC-04 cover actor threading, null fallback, and ledger exposure",
      "current_or_option_state": "TASK-01",
      "gap_or_implication": "Integration test pattern from existing `stockInflows.server.test.ts`",
      "carry_forward_or_notes": null
    },
    {
      "coverage_area": "Data / contracts",
      "applicable": "`StockAdjustmentActor` and `StockInflowActor` gain `ip?: string`; `LedgerEvent` gains `operatorId: string \\",
      "current_or_option_state": "null`; Zod nested schemas unchanged (not `.strict()`)",
      "gap_or_implication": "TASK-01",
      "carry_forward_or_notes": null
    },
    {
      "coverage_area": "Performance / reliability",
      "applicable": "IP extraction is synchronous; one extra field in DB write; no query changes",
      "current_or_option_state": "-",
      "gap_or_implication": "N/A — negligible overhead",
      "carry_forward_or_notes": null
    },
    {
      "coverage_area": "Rollout / rollback",
      "applicable": "No feature flag; additive change; rollback = code revert; existing null rows are valid state",
      "current_or_option_state": "TASK-01",
      "gap_or_implication": "No migration to reverse",
      "carry_forward_or_notes": null
    }
  ],
  "open_operator_questions": [],
  "stage_payload": {
    "summary": "Stock adjustments and inflows currently write `operatorId: null` to every `InventoryAuditEvent` row, even though the column already exists and the repository functions already accept an optional `actor` parameter. This plan threads the requester's IP address from the HTTP request headers through the route handler → repository → database write path using the existing `getTrustedRequestIpFromHeaders` utility. The `LedgerEvent` type gains an `operatorId` field so the attribution is visible in the ledger GET response. No DB migration is needed. The `StockAdjustmentActor` and `StockInflowActor` typ",
    "goals": [
      "Every `POST /api/inventory/[shop]/adjustments` write records the requester IP in `operatorId`.",
      "Every `POST /api/inventory/[shop]/inflows` write records the requester IP in `operatorId`.",
      "The ledger `GET /api/inventory/[shop]/ledger` response exposes `operatorId` in each event.",
      "No unauthenticated request can supply a fabricated actor identity (IP is server-side derived)."
    ],
    "non_goals": [
      "Adding per-user login or per-staff accounts; single-token model unchanged.",
      "UI changes to display attribution (API response exposure is sufficient for this change).",
      "Retroactive backfill of historical `operatorId = null` rows."
    ],
    "constraints": [
      "Session token carries no user identity; IP is the only server-side attribution source.",
      "`getTrustedRequestIpFromHeaders` returns `\"\"` when `INVENTORY_TRUST_PROXY_IP_HEADERS` is unset — this is stored as `null` (falsy empty string maps to null gracefully)."
    ],
    "assumptions": [
      "IP-based attribution (confirmed as default by operator decision per fact-find).",
      "`options?.actor?.ip ?? null` (or empty string coerced to null) is the value stored in `operatorId`."
    ],
    "selected_approach": [
      "What was chosen:",
      "Extract requester IP in both route handlers using `getTrustedRequestIpFromHeaders(req.headers)`.",
      "Build an actor object `{ ip }` and pass as `options` third argument to `applyStockAdjustment` / `receiveStockInflow`.",
      "In both repositories, replace `operatorId: null` with `options?.actor?.ip || null`.",
      "Add `ip?: string` to both actor types.",
      "Add `operatorId: string | null` to `LedgerEvent` type and map it in the ledger GET route.",
      "Why planning is not reopening option selection:",
      "Operator confirmed IP-based approach (fact-find open question defaulted to IP). All code extension points exist. No alternative approach adds value without auth infrastructure changes."
    ],
    "plan_gates": {
      "Foundation Gate": "Pass",
      "Sequenced": "Yes",
      "Edge-case review complete": "Yes",
      "Auto-build eligible": "Yes"
    },
    "task_briefs": [
      {
        "task_id": "TASK-01",
        "title": "Thread IP-based actor attribution through adjustments and inflows routes",
        "type": "IMPLEMENT",
        "status": "Pending",
        "confidence": 87,
        "effort": "S",
        "depends_on": [],
        "blocks": [],
        "execution_skill": "lp-do-build",
        "deliverable": "code-change — updated type files, repository files, route handlers, inventory-utils.ts",
        "affects": null
      }
    ],
    "next_runnable_task_ids": [
      "TASK-01"
    ],
    "validation_contracts": [],
    "open_decisions": [],
    "process_topology": {
      "changed": false,
      "note": "None: no material process topology change.",
      "areas": []
    }
  }
}
