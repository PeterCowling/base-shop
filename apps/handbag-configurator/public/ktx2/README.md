# KTX2 transcoders

This directory ships the Basis/KTX2 transcoder files from
`three/examples/jsm/libs/basis/` so `KTX2Loader` can decode compressed textures.
Update these files when upgrading `three`.

- `basis_transcoder.js`
- `basis_transcoder.wasm`
