# @acme/cms-ui (Archived)

This package is not currently part of the active UI surface area in this repo.

- Do not add new imports from `@acme/cms-ui`.
- Use `@acme/ui/components/cms` and related `@acme/ui/*` entrypoints instead.

The build/lint/typecheck scripts are intentionally skipped while this package is inactive.
