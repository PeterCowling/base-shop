/**
 * JSON-LD structured data components for Skylar.
 *
 * Uses @acme/seo/jsonld builders for Schema.org-compliant structured data
 * with XSS-safe serialization.
 */
export { JsonLdScript, organizationJsonLd } from "@acme/seo/jsonld";
