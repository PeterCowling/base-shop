type Props = {
    locale: "en" | "de" | "it";
    taxRegion: string;
    coverage?: string[];
};
export default function CheckoutForm({ locale, taxRegion, coverage, }: Props): import("react/jsx-runtime").JSX.Element;
export {};
