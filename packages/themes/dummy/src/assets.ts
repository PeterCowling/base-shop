import type { ThemeAssets } from "@themes/base";

/** Dummy theme has no brand assets. Exists as a minimal test/placeholder theme. */
export const assets: ThemeAssets = {
  fonts: {},
  gradients: {},
  shadows: {},
  keyframes: {},
  brandColors: {},
};
