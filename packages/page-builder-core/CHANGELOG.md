# Changelog

## 1.0.0
- Start semver at 1.0.0 for the public Page Builder core surface.
- Lock the shared block registry contract (`BlockTypeId`, descriptors, `buildBlockRegistry`, `coreBlockDescriptors`) for CMS/runtime consumers.
- Document the exported history, diff, and schema helpers as the stable API for PB integrations.
