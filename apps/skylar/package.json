{
  "name": "@apps/skylar",
  "private": true,
  "scripts": {
    "pretypecheck": "pnpm --filter @acme/i18n build",
    "prelint": "pnpm --filter @acme/eslint-plugin-ds build",
    "dev": "next dev -p 3008",
    "build": "next build",
    "start": "next start -p 3008",
    "lint": "eslint .",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "pnpm exec jest --ci --runInBand --detectOpenHandles --config ./jest.config.cjs"
  },
  "dependencies": {
    "@acme/config": "workspace:*",
    "@acme/i18n": "workspace:*",
    "@acme/next-config": "workspace:*",
    "@acme/tailwind-config": "workspace:*",
    "next": "^15.3.8",
    "react": "19.1.0",
    "react-dom": "19.1.0",
    "zod": "^3.25.73"
  }
}
