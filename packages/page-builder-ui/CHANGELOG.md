# Changelog

## 1.0.0
- Start semver at 1.0.0 for the Page Builder UI surface.
- Stabilise the editor shell exports (PageBuilder, PageBuilderLayout, toolbar/canvas/sidebar primitives) and supporting hooks.
- Re-export the starter block registry built from `coreBlockDescriptors` and default UI implementations.
