#!/usr/bin/env node

/**
 * Entry point for the `configurator` CLI.
 *
 * This tiny wrapper exists so that the bin target declared in
 * `package.json` always points to a file that exists at install
 * time. The actual implementation resides in the built output under
 * `dist/index.js`, which is generated by the TypeScript compiler
 * during the `build` step triggered via the `prepack` script.
 */

import("../dist/index.js").catch((err) => {
  console.error("Failed to load the compiled configurator module:", err);
  process.exitCode = 1;
});
