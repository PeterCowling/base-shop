/* File: /src/hooks/orchestrations/bar/actions/clientActions/useGridColumns.ts */

/** Returns how many columns the ProductGrid should have (hard-coded to 6). */
export default function useGridColumns(): number {
  return 6;
}
