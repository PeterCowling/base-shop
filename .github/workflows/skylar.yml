name: Deploy Skylar

on:
  push:
    branches:
      - main
      - staging
    paths:
      - 'apps/skylar/**'
      - 'packages/config/**'
      - 'packages/i18n/**'
      - 'packages/next-config/**'
      - 'packages/tailwind-config/**'
      - 'packages/ui/**'
      - 'packages/platform-core/**'
      - 'packages/shared-utils/**'
      - 'packages/themes/**'
      - 'packages/design-tokens/**'
      - '.github/workflows/skylar.yml'
  pull_request:
    paths:
      - 'apps/skylar/**'
      - 'packages/config/**'
      - 'packages/i18n/**'
      - 'packages/next-config/**'
      - 'packages/tailwind-config/**'
      - 'packages/ui/**'
      - 'packages/platform-core/**'
      - 'packages/shared-utils/**'
      - 'packages/themes/**'
      - 'packages/design-tokens/**'
      - '.github/workflows/skylar.yml'
  workflow_dispatch:

jobs:
  deploy:
    uses: ./.github/workflows/reusable-app.yml
    with:
      app-filter: "@apps/skylar"
      # Use the shared static-export mode for Skylar so that
      # next/image renders plain <img> tags and the output in
      # apps/skylar/out is fully compatible with Cloudflare Pages.
      build-cmd: OUTPUT_EXPORT=1 pnpm --filter @apps/skylar... build
      artifact-path: apps/skylar/out
      deploy-cmd: >
        pnpm exec wrangler pages deploy apps/skylar/out --project-name skylar --branch ${{ github.ref_name }}
      project-name: "skylar"
      environment-name: "${{ github.ref_name == 'staging' && 'staging' || 'production' }}"
      environment-url: "${{ github.ref_name == 'staging' && 'https://staging.skylar.pages.dev' || 'https://skylar.pages.dev' }}"
      healthcheck-args: "${{ github.ref_name == 'staging' && '--staging' || '' }}"
    secrets: inherit
