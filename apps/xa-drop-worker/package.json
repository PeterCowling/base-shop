{
  "name": "@apps/xa-drop-worker",
  "private": true,
  "scripts": {
    "dev": "wrangler dev src/index.ts --local --port 8792",
    "build": "wrangler build",
    "lint": "eslint .",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "bash ../../scripts/tests/run-governed-test.sh -- jest -- --config ./jest.config.cjs"
  }
}
