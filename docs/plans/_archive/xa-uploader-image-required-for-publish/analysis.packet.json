{
  "schema_version": "do-stage-handoff.v2",
  "stage": "analysis",
  "feature_slug": "xa-uploader-image-required-for-publish",
  "source_artifact_path": "docs/plans/xa-uploader-image-required-for-publish/analysis.md",
  "source_artifact_status": "Draft",
  "source_last_updated": "2026-03-12T00:00:00.000Z",
  "execution_track": "code",
  "deliverable_type": "code-change",
  "primary_execution_skill": "lp-do-build",
  "supporting_skills": [
    "none"
  ],
  "related_artifacts": {
    "fact_find": "docs/plans/xa-uploader-image-required-for-publish/fact-find.md",
    "plan": "docs/plans/xa-uploader-image-required-for-publish/plan.md"
  },
  "outcome_contract": {
    "why": "When staff add products using the main upload workflow, products can appear live in the store without images, which looks unprofessional and loses sales — shoppers browsing a luxury fashion brand expect visuals for everything.",
    "intended_outcome_type": "operational",
    "intended_outcome_statement": "Products cannot go live without at least one image.",
    "source": "operator"
  },
  "engineering_coverage": [
    {
      "coverage_area": "UI / visual",
      "applicable": "Inline `imageFiles` field error set via `setFieldErrors`; existing error display renders it",
      "current_or_option_state": "Disabled/error state on Save button — requires form-layer conditional logic",
      "gap_or_implication": "A: re-uses existing field-error channel without new UI components",
      "carry_forward_or_notes": null
    },
    {
      "coverage_area": "UX / states",
      "applicable": "Pre-save block; user sees field error immediately on Save attempt",
      "current_or_option_state": "Block at button level; earlier feedback",
      "gap_or_implication": "A is consistent with existing form validation UX pattern",
      "carry_forward_or_notes": null
    },
    {
      "coverage_area": "Security / privacy",
      "applicable": "N/A — admin-only, no auth surface change",
      "current_or_option_state": "N/A",
      "gap_or_implication": "None",
      "carry_forward_or_notes": null
    },
    {
      "coverage_area": "Logging / observability / audit",
      "applicable": "Add `uploaderLog(\"warn\", \"publish_blocked_no_images\", {...})` in the guard branch",
      "current_or_option_state": "Same pattern possible",
      "gap_or_implication": "A is the right layer for action-level observability",
      "carry_forward_or_notes": null
    },
    {
      "coverage_area": "Testing / validation",
      "applicable": "TC-03 adds to `catalogConsoleActions.test.ts` B1 suite using existing `fetchMock` + `setFieldErrors` mock",
      "current_or_option_state": "Requires new component test; `CatalogProductForm.test.tsx` exists but is heavier to extend",
      "gap_or_implication": "A: straightforward TC-03 in existing harness",
      "carry_forward_or_notes": null
    },
    {
      "coverage_area": "Data / contracts",
      "applicable": "No schema or server changes; server normalization already in place",
      "current_or_option_state": "Same",
      "gap_or_implication": "No data contract change",
      "carry_forward_or_notes": null
    },
    {
      "coverage_area": "Performance / reliability",
      "applicable": "Single synchronous pre-check before fetch; negligible",
      "current_or_option_state": "Same",
      "gap_or_implication": "None",
      "carry_forward_or_notes": null
    },
    {
      "coverage_area": "Rollout / rollback",
      "applicable": "One function changed; clean git revert",
      "current_or_option_state": "Two layers changed",
      "gap_or_implication": "A is simpler to roll back",
      "carry_forward_or_notes": null
    }
  ],
  "open_operator_questions": [],
  "stage_payload": {
    "decision_summary": "The fact-find described a data integrity bug: products with no images could be persisted as `publishState: \"live\"` when a staff member confirmed the `would_unpublish` dialog. Analysis investigation reveals this is **not accurate** — `route.ts:286` already calls `deriveCatalogPublishState(productInput)` at persist time, which always returns `\"draft\"` when `imageFiles` is empty. The server was hardened in a prior plan (`xa-uploader-media-path-and-publish-flow-hardening`). The real remaining gap is UX-only: the client has no pre-save hard gate. Staff who set `publishState: \"live\"` with no images ",
    "goals": [
      "Add a hard client-side gate that blocks a \"publish without images\" save attempt with an inline error.",
      "Preserve the `would_unpublish` confirmation flow as a server-side fallback (it is still useful as a last-resort guard).",
      "Ensure TC-01 and TC-02 regression tests are written against the existing correct server behavior.",
      "Ensure TC-03 tests the new client-side gate."
    ],
    "non_goals": [
      "Server-side changes — the server already normalizes `publishState` via `deriveCatalogPublishState`.",
      "Storefront no-image fallback rendering.",
      "Image upload UI, R2 endpoint, or CDN changes.",
      "Any other xa-uploader feature work."
    ],
    "constraints": [
      "Tests run in CI only.",
      "English-only — internal tool.",
      "`catalogConsoleActions.test.ts` has an established `handleSaveImpl` + `fetchMock` harness.",
      "The server's `deriveCatalogPublishState` normalization must NOT be removed — it is defense-in-depth."
    ],
    "assumptions": [
      "Staff can always save as \"draft\" (no images required for draft).",
      "A live product with images being saved normally should never trigger the new gate."
    ],
    "evaluation_criteria": [
      {
        "Criterion": "Blast radius",
        "Why it matters": "Narrow is mandatory — only the save action should change",
        "Weight/priority": "Critical"
      },
      {
        "Criterion": "Test coverage",
        "Why it matters": "TC-03 (client gate) must be verifiable with the existing harness",
        "Weight/priority": "High"
      },
      {
        "Criterion": "UX clarity",
        "Why it matters": "Staff need an explicit message, not a confusing dialog for this case",
        "Weight/priority": "High"
      },
      {
        "Criterion": "Defense-in-depth",
        "Why it matters": "Server normalization must remain intact regardless of client changes",
        "Weight/priority": "Medium"
      },
      {
        "Criterion": "Implementation simplicity",
        "Why it matters": "Small team; avoid over-engineering",
        "Weight/priority": "Medium"
      }
    ],
    "options_considered": [
      {
        "Option": "A",
        "Description": "Client-side hard gate in `handleSaveImpl`: check `publishState === \"live\" && imageFiles empty` before `doSave()`; set inline `imageFiles` field error and return `{ status: \"error\" }`",
        "Upside": "Minimal — one check before the save attempt; testable with existing harness; no new files",
        "Downside": "None significant",
        "Key risks": "None — the check uses already-available draft fields",
        "Viable?": "Yes"
      },
      {
        "Option": "B",
        "Description": "UI-layer gate in `CatalogProductForm`: disable/error the Save button when `publishState === \"live\" && !hasImages`",
        "Upside": "Earlier in the stack; gives visual feedback before user clicks Save",
        "Downside": "Requires component-level testing; harder to write a precise TC; more coupling",
        "Key risks": "May interact with autosave logic if autosave bypasses the form gate",
        "Viable?": "Yes"
      },
      {
        "Option": "C",
        "Description": "Client gate (A) + rewrite `would_unpublish` dialog text to distinguish the two cases",
        "Upside": "Better UX documentation",
        "Downside": "Additional string/i18n scope; dialog is already effectively dead-code for this path after A is added",
        "Key risks": "Minor scope creep",
        "Viable?": "Yes but out of scope"
      }
    ],
    "chosen_approach": {
      "Recommendation": "Option A — client-side hard gate in `handleSaveImpl`.",
      "Why this wins": "Narrowest blast radius. One check in one function, testable with the existing harness, uses the already-available `setFieldErrors` callback and `splitList` utility. Option B provides earlier visual feedback but requires component-layer changes with heavier test infrastructure and creates an implicit dependency between form state and button affordance that the existing save action flow doesn't need. The server already handles the data integrity case; the fix needed is UX clarity, and Option A delivers that at the right abstraction layer.",
      "**What it depends on": "**",
      "Option B (UI gate in form) — viable but heavier": "changes the component layer unnecessarily when the action layer already has the right callback contract for field errors.",
      "Option C (dialog rewrite) — out of scope": "the `would_unpublish` dialog will no longer fire for the \"no images\" case once Option A is in place, making the rewrite low-value and adds i18n surface."
    },
    "rejected_approaches": [
      "Option B (UI gate in form) — viable but heavier: changes the component layer unnecessarily when the action layer already has the right callback contract for field errors.",
      "Option C (dialog rewrite) — out of scope: the `would_unpublish` dialog will no longer fire for the \"no images\" case once Option A is in place, making the rewrite low-value and adds i18n surface."
    ],
    "planning_handoff": {
      "planning_focus": [],
      "validation_implications": [],
      "sequencing_constraints": [
        "No external dependencies. TASK-01 and TASK-02 can be written together in a single commit."
      ],
      "risks_to_carry_forward": [
        "`splitList` import path in `catalogConsoleActions.ts` — needs to confirm the utility is accessible without new imports."
      ]
    },
    "process_topology": {
      "changed": true,
      "note": null,
      "areas": [
        {
          "id": null,
          "label": "Save attempt — imageless publish",
          "current": "`handleSaveImpl` proceeds to `doSave()` → POST → 409 `would_unpublish` → confirmation dialog → retry with `confirmUnpublish: true` → server saves as \"draft\" (silent downgrade)",
          "trigger": "Staff clicks Save with `publishState: \"live\"` and `imageFiles: \"\"`",
          "future": "`handleSaveImpl` checks `publishState === \"live\" && imageFiles empty` before `doSave()` → `setFieldErrors({ imageFiles: \"<error message>\" })` → returns `{ status: \"error\" }` → no POST made",
          "steady": "409 `would_unpublish` server path remains as last-resort guard; server normalization unchanged",
          "seams": "None — the new check is purely additive"
        },
        {
          "id": null,
          "label": "Save attempt — live product with images",
          "current": "Unchanged — products with images save normally",
          "trigger": "Staff saves product with `publishState: \"live\"` and `imageFiles` non-empty",
          "future": "Unchanged",
          "steady": "All save paths",
          "seams": "None"
        },
        {
          "id": null,
          "label": "Save attempt — draft product with no images",
          "current": "Unchanged — can always save as draft",
          "trigger": "Staff saves product with `publishState: \"draft\"`",
          "future": "Unchanged",
          "steady": "All draft save paths",
          "seams": "None"
        },
        {
          "id": null,
          "label": "Server persist normalization",
          "current": "`route.ts:286` calls `deriveCatalogPublishState(productInput)` — already normalizes",
          "trigger": "Any POST to `/api/catalog/products`",
          "future": "Unchanged — no server changes in this plan",
          "steady": "Server normalization remains defense-in-depth",
          "seams": "None"
        }
      ]
    }
  }
}
