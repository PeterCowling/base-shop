// tsconfig.json
{
  "files": [],
  "extends": "./tsconfig.base.json",

  /* ────────── project references ────────── */
  "references": [
    { "path": "./packages/types" },
    { "path": "./tsconfig.packages.json" },
    { "path": "./apps/cms" },
    { "path": "./apps/shop-abc" },
    { "path": "./apps/shop-bcd" },
    { "path": "./scripts" }
  ],

  /* NEW: keep tests & demo shops out of the **production** type-check.
           The dedicated tsconfig.test.json already handles test files.  */
  "exclude": [
    "**/__tests__/**",
    "**/*.test.ts",
    "**/*.test.tsx",
    "apps/shop-abc",
    "apps/shop-bcd",
    "apps-script"
  ]
}
