# Brikette Cloudflare Workers deployment config (legacy — production now uses Pages)
# Kept for reference. Production and staging both deploy via `wrangler pages deploy`.
name = "brikette-staging"
main = ".open-next/worker.js"
compatibility_date = "2025-06-20"
compatibility_flags = ["nodejs_compat"]

[assets]
directory = ".open-next/assets"
binding = "ASSETS"
