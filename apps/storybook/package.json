{
  "name": "@apps/storybook",
  "private": true,
  "scripts": {
    "dev": "storybook dev -p 6006 -c ./.storybook",
    "dev:ci": "storybook dev -p 6007 --ci -c ./.storybook-ci",
    "dev:composed": "storybook dev -p 6012 -c ./.storybook-composed",
    "build": "echo 'Storybook build skipped - webpack hash bug'",
    "build:full": "storybook build -c ./.storybook",
    "build:ci": "storybook build -c ./.storybook-ci",
    "build:composed": "storybook build -c ./.storybook-composed",
    "lint": "echo 'noop'"
  }
}
