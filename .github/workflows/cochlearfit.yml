name: Deploy Cochlearfit

on:
  push:
    branches:
      - main
    paths:
      - 'apps/cochlearfit/**'
      - 'packages/i18n/**'
      - 'packages/next-config/**'
      - 'packages/tailwind-config/**'
      - '.github/workflows/cochlearfit.yml'
  pull_request:
    paths:
      - 'apps/cochlearfit/**'
      - 'packages/i18n/**'
      - 'packages/next-config/**'
      - 'packages/tailwind-config/**'
      - '.github/workflows/cochlearfit.yml'
  workflow_dispatch:
    inputs:
      deploy-target:
        description: 'Deploy target environment'
        required: true
        default: 'staging'
        type: choice
        options:
          - staging
          - production

jobs:
  # Auto-deploy to staging on push to main
  staging:
    if: github.event_name == 'push' || (github.event_name == 'workflow_dispatch' && inputs.deploy-target == 'staging')
    uses: ./.github/workflows/reusable-app.yml
    with:
      app-filter: "@apps/cochlearfit"
      build-cmd: pnpm --filter @apps/cochlearfit... build
      artifact-path: ""
      deploy-cmd: >
        cd apps/cochlearfit && pnpm exec next-on-pages deploy --project-name cochlearfit --branch staging
      project-name: "cochlearfit"
      target-environment: staging
    secrets: inherit

  # Manual production deployment (requires GitHub environment approval)
  production:
    if: github.event_name == 'workflow_dispatch' && inputs.deploy-target == 'production'
    uses: ./.github/workflows/reusable-app.yml
    with:
      app-filter: "@apps/cochlearfit"
      build-cmd: pnpm --filter @apps/cochlearfit... build
      artifact-path: ""
      deploy-cmd: >
        cd apps/cochlearfit && pnpm exec next-on-pages deploy --project-name cochlearfit --branch main
      project-name: "cochlearfit"
      target-environment: production
    secrets: inherit

  # Validation only for PRs (no deploy)
  validate:
    if: github.event_name == 'pull_request'
    uses: ./.github/workflows/reusable-app.yml
    with:
      app-filter: "@apps/cochlearfit"
      build-cmd: pnpm --filter @apps/cochlearfit... build
      artifact-path: ""
      project-name: "cochlearfit"
      target-environment: staging
    secrets: inherit
