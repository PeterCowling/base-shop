{
  "schema_version": "do-stage-handoff.v2",
  "stage": "analysis",
  "feature_slug": "prime-ds-rules-deferred",
  "source_artifact_path": "docs/plans/prime-ds-rules-deferred/analysis.md",
  "source_artifact_status": "Ready-for-planning",
  "source_last_updated": "2026-03-13T00:00:00.000Z",
  "execution_track": "code",
  "deliverable_type": "code-change",
  "primary_execution_skill": "lp-do-build",
  "supporting_skills": [
    "none"
  ],
  "related_artifacts": {
    "fact_find": "docs/plans/prime-ds-rules-deferred/fact-find.md",
    "plan": "docs/plans/prime-ds-rules-deferred/plan.md"
  },
  "outcome_contract": {
    "why": "Three pages in the prime app have design system rules disabled. These rules enforce minimum button and tap target sizes and consistent layout widths. Without them, these pages may have elements that are hard to tap on mobile, inconsistently laid out, or visually misaligned compared to the rest of the app. The disables are marked as deferred — they were never resolved.",
    "intended_outcome_type": "operational",
    "intended_outcome_statement": "All 24 unique BRIK-3 files (22 file-level suppressions + 2 inline-only files) pass DS lint rules with no suppressions. `pnpm --filter prime lint -- --full` returns zero DS rule violations across the entire Prime codebase.",
    "source": "operator"
  },
  "engineering_coverage": [
    {
      "coverage_area": "UI / visual",
      "applicable": "~80–120 line changes across 24 files; visual parity preserved via class equivalence",
      "current_or_option_state": "~10–15 line changes on 2 files",
      "gap_or_implication": "No change",
      "carry_forward_or_notes": "Must verify no visual regressions on key screens after button height and container changes"
    },
    {
      "coverage_area": "UX / states",
      "applicable": "Buttons gain correct 40px minimum tap target on all affected pages",
      "current_or_option_state": "Buttons fixed on 2 pages only; 21 pages remain broken",
      "gap_or_implication": "No UX improvement",
      "carry_forward_or_notes": "Add `min-h-10 min-w-10` (or `size-10`) to every violating interactive element; `w-full` alone does not satisfy the rule"
    },
    {
      "coverage_area": "Security / privacy",
      "applicable": "N/A",
      "current_or_option_state": "N/A",
      "gap_or_implication": "N/A",
      "carry_forward_or_notes": "N/A"
    },
    {
      "coverage_area": "Logging / observability / audit",
      "applicable": "N/A",
      "current_or_option_state": "N/A",
      "gap_or_implication": "N/A",
      "carry_forward_or_notes": "N/A"
    },
    {
      "coverage_area": "Testing / validation",
      "applicable": "24 files with lint violations fixed; CI lint gate restored; targeted test assertions for tap-size on key buttons",
      "current_or_option_state": "2 files only; CI lint still suppressed on 21 files",
      "gap_or_implication": "No testing improvement",
      "carry_forward_or_notes": "Add class assertions for `min-h-10 min-w-10` on fixed interactive elements in existing test files"
    },
    {
      "coverage_area": "Data / contracts",
      "applicable": "N/A — purely UI changes",
      "current_or_option_state": "N/A",
      "gap_or_implication": "N/A",
      "carry_forward_or_notes": "N/A"
    },
    {
      "coverage_area": "Performance / reliability",
      "applicable": "N/A",
      "current_or_option_state": "N/A",
      "gap_or_implication": "N/A",
      "carry_forward_or_notes": "N/A"
    },
    {
      "coverage_area": "Rollout / rollback",
      "applicable": "Single PR to dev; no feature flag; rollback = revert PR",
      "current_or_option_state": "Same",
      "gap_or_implication": "No PR needed",
      "carry_forward_or_notes": "Confirm CI lint passes cleanly before merge; no migration ordering constraints"
    }
  ],
  "open_operator_questions": [],
  "stage_payload": {
    "decision_summary": "24 unique files in the Prime guest app carry BRIK-3-labelled DS lint suppressions (22 with file-level `/* eslint-disable */` blocks and 2 with inline-only BRIK-3 disables; `digital-assistant/page.tsx` carries both a file-level and an inline instance). Three DS lint rules are suppressed: `ds/container-widths-only-at`, `ds/enforce-layout-primitives`, and `ds/min-tap-size`. These were deferred during the initial Prime build sprint under label \"BRIK-3\" and were never resolved. The decision to make now: **fix all violations in one pass, fix a subset, or formally defer with a TTL**. This analysis re",
    "goals": [
      "Select between full remediation, partial fix, or formal deferral.",
      "Confirm the per-rule fix patterns are correct and safe.",
      "Establish scope, sequencing, and validation contract for planning."
    ],
    "non_goals": [
      "BRIK-2 (meal-orders), PLAT-ENG-0001, or any non-BRIK-3 disables.",
      "Backend, data model, or API changes.",
      "Broader DS primitive adoption beyond what's needed to clear the violations."
    ],
    "constraints": [
      "`@acme/design-system/primitives` exports Stack, Inline, Grid, Cluster, Cover, Sidebar, Button, Card — but NOT Page, Section, Container, or Overlay.",
      "Prime has a local `Container` component at `apps/prime/src/components/layout/Container.tsx` (exports `Container` function wrapping `<div className=\"mx-auto w-full max-w-6xl\">`). This component name is on the `container-widths-only-at` rule's allowlist.",
      "`ds/min-tap-size` requires BOTH `min-h-X` AND `min-w-X` numeric scale (or `size-X`) ≥ 10 (40px). `w-full`, padding, and text width do not count.",
      "`ds/enforce-layout-primitives` only fires on leaf JSX elements (no JSXElement children).",
      "Tests run in CI only (testing policy)."
    ],
    "assumptions": [
      "No operator-preference constraint prevents fixing all 24 files in one PR; the operator question in the fact-find defaults to \"fix all\".",
      "Adding `min-h-10 min-w-10` to buttons adds ~4px minimum height — a small but non-zero visual change. Not zero-risk, but low-risk and verifiable with spot checks.",
      "Prime's local `Container` component (`apps/prime/src/components/layout/Container.tsx`) injects `w-full max-w-6xl` plus any override. This is NOT identical DOM to a bare `<div className=\"mx-auto max-w-md\">` — the `Container` adds `w-full` and uses `max-w-6xl` as base, with the override in `className`. For pages needing `max-w-md`, `<Container className=\"!max-w-md\">` or simply passing `className=\"mx-auto max-w-md\"` (which wins over the base due to Tailwind order) is the fix. This is a small structural refactor, not a drop-in swap; planning must treat this as a visual verification item."
    ],
    "evaluation_criteria": [
      {
        "Criterion": "Lint gate cleanliness",
        "Why it matters": "Suppressions hide real violations from CI; removing them restores enforcement",
        "Weight/priority": "High"
      },
      {
        "Criterion": "Mobile UX correctness",
        "Why it matters": "Buttons under 40px are hard to tap on phones used by hostel guests",
        "Weight/priority": "High"
      },
      {
        "Criterion": "Visual regression risk",
        "Why it matters": "Adding height constraints or wrapping divs could cause reflow",
        "Weight/priority": "Medium"
      },
      {
        "Criterion": "Per-file effort",
        "Why it matters": "Determines PR size and review burden",
        "Weight/priority": "Medium"
      },
      {
        "Criterion": "Test auditability",
        "Why it matters": "Changes should be verifiable in CI",
        "Weight/priority": "Medium"
      },
      {
        "Criterion": "Future debt",
        "Why it matters": "Formal deferral without TTL leaves violations silently accumulating",
        "Weight/priority": "High"
      }
    ],
    "options_considered": [
      {
        "Option": "A — Fix all 24 files in one PR",
        "Description": "Remove all BRIK-3 disables, fix every revealed violation using established patterns",
        "Upside": "Lint gate fully restored; no accumulated debt; single clean PR",
        "Downside": "Larger diff (~24 files, ~80–120 line changes); more review surface",
        "Key risks": "Visual regression on any individual button/container change",
        "Viable?": "Yes"
      },
      {
        "Option": "B — Fix dispatch-original 3 files only",
        "Description": "Fix only ActivitiesClient, GuestDirectory (already clean), StaffLookupClient",
        "Upside": "Smallest diff; matches dispatch scope literally",
        "Downside": "Leaves 21 files still suppressed; does not meaningfully restore lint enforcement",
        "Key risks": "BRIK-3 label persists across the app; future developers confused about which files are intended vs. accidental suppressions",
        "Viable?": "Yes (but inferior)"
      },
      {
        "Option": "C — Formal deferral with TTL",
        "Description": "Keep disables, update comments to add TTL (e.g. `[ttl=2026-12-31]`) and a link to this plan",
        "Upside": "Preserves status quo; zero regression risk",
        "Downside": "Does not fix the underlying violations; tap-size problems remain on mobile; TTL requires follow-up discipline",
        "Key risks": "TTL expires unnoticed; violations accumulate further",
        "Viable?": "No — adds process overhead without fixing the problem"
      }
    ],
    "chosen_approach": {
      "Recommendation": "Option A — Fix all 24 files in one PR.",
      "Why this wins": "The per-file cost is low (1–5 class additions or a wrapper component swap), the fix patterns are established, and fixing all 24 in one pass restores the lint gate completely. Fixing a subset (Option B) leaves the majority of suppressions in place and provides no material improvement to CI enforcement. Option C defers without fixing and introduces TTL tracking overhead with no UX benefit.",
      "**What it depends on": "**"
    },
    "rejected_approaches": [
      "**Option B (fix 3 files)** — Leaves 21 files suppressed with no enforcement improvement. The dispatch identified 3 files as the symptom, but the root cause spans 24. Given the low per-file cost, partial fixing is not justified.",
      "**Option C (formal deferral with TTL)** — Adds process overhead without closing any violations. Mobile tap-size problems remain on all affected pages. Rejected because it delays rather than resolves the problem."
    ],
    "planning_handoff": {
      "planning_focus": [],
      "validation_implications": [
        "`pnpm --filter prime lint -- --full` must return zero DS violations across the full Prime codebase (not just changed files). Default `pnpm lint` only lints changed files per `lint-wrapper.sh` — this is insufficient for acceptance.",
        "Visual spot-check on key pages (Activities, StaffLookup, BookingDetails, DigitalAssistant) after button height changes and Container swap. The Container swap is a small structural refactor (adds `w-full` base class), not a guaranteed visual no-op."
      ],
      "sequencing_constraints": [
        "Lint dry-run task first (enumerate violations) → then 3 parallel fix tracks → then test additions → then final lint validation.",
        "No dependency on external services or data models."
      ],
      "risks_to_carry_forward": [
        "Exact violation count per file is not known until the dry-run; plan must budget a lint-fix pass rather than specifying exact line counts.",
        "`enforce-layout-primitives` leaf-only check means some apparent flex elements may not actually be flagged; the dry-run will confirm."
      ]
    },
    "process_topology": {
      "changed": false,
      "note": "None: no material process topology change. The chosen approach removes lint suppression comments and fixes class/wrapper patterns in 24 source files. CI lint gates operate identically before and after; the only difference is that violations are no longer hidden. No workflows, runbooks, lifecycle states, or deploy lanes are altered.",
      "areas": []
    }
  }
}
