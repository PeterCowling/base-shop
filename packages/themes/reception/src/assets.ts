import type { ThemeAssets } from "@themes/base";

/**
 * Reception brand assets.
 * Hospitality operational interface — uses Inter via base,
 * no custom fonts or gradients. Shade color families are in tokens.
 */
export const assets: ThemeAssets = {
  fonts: {},
  gradients: {},
  shadows: {},
  keyframes: {},
  brandColors: {},
};
