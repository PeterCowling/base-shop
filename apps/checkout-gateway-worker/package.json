{
  "name": "@apps/checkout-gateway-worker",
  "private": true,
  "scripts": {
    "dev": "wrangler dev src/index.ts --local --port 8790",
    "build": "wrangler build",
    "lint": "eslint .",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
